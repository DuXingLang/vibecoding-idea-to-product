---
name: vibecoding-idea-to-product
description: >
  End-to-end workflow from product idea to shipped product.
  Covers PRD, UI prototype, tech design, small-step development,
  testing, and deployment. Use when the user wants to build a product
  from scratch, turn an idea into a shippable app, or resume an
  idea-to-product workflow. 从想法到产品的全流程规范：PRD、UI 原型、
  技术方案、小步迭代开发、测试与部署。
version: 2.0.0
license: MIT
compatibility: Claude Code, Codex, TRAE, Zcode, Cursor, generic agent
triggers:
  - 我有一个想法
  - 我想做一个产品
  - 帮我从零开发一个
  - 我要从想法到上线
  - 我们一起做个产品吧
  - 从零开始做一个项目
  - 恢复进度
  - I have an idea
  - I want to build a product
  - Build a product from scratch
  - From idea to launch
  - Resume progress
metadata:
  author: DuXingLang
  category: workflow
  tags: [prd, prototype, architecture, iterative-dev, shipping]
---

# 从想法到产品全流程（VibeCoding Idea → Product）

当用户提出产品想法、希望从零做出可交付产品，或要求恢复 idea-to-product 进度时，**严格按本 Skill 推进**。

本 Skill 为**单文件通用**规范，兼容 Claude Code、Codex、TRAE、Zcode、Cursor 及通用 Agent。动作用平台中立描述：**读取文件 / 写入文件 / 运行命令 / 询问用户**。

## 核心原则

1. **门禁优先**：每阶段结束必须获得用户**明确确认**，方可进入下一阶段
2. **文档驱动**：代码服从已确认的 `docs/` 文档；变更先改文档再改代码
3. **单任务聚焦**：开发阶段一次只做一个 TODO 项
4. **平台中立**：不假设特定 IDE/CLI 工具名
5. **双语友好**：用户用中文则中文交互；用英文则英文；默认文档为简体中文
6. **安全底线**：禁止硬编码密钥；认证/支付等核心逻辑列入不可擅自修改清单
7. **可恢复**：启动时扫描 `docs/` 与 `TODO.md` 判断进度，展示推断并请用户确认

**产出目录约定**：运行时产物写在**用户当前项目**的 `docs/`（及根目录 `TODO.md`）。若本 Skill 仓库自带说明性 `docs/`，勿与用户项目产物混淆。

可选模板（若 Skill 安装包内提供）：`docs/templates/*.template.md`。写产物时优先对齐模板标题层级；无模板时按本文件结构即可。

---

## 目录

- [Phase 0：前置检查与路由](#phase-0前置检查与路由)
- [模式：Full Flow 与 Fast Track](#模式full-flow-与-fast-track)
- [阶段 1：产品定位](#阶段-1产品定位)
- [阶段 2：视觉设计](#阶段-2视觉设计)
- [阶段 3：技术方案](#阶段-3技术方案)
- [阶段 4：准备开发](#阶段-4准备开发)
- [阶段 5：正式开发](#阶段-5正式开发)
- [阶段 6：系统测试](#阶段-6系统测试)
- [阶段 7：部署交付](#阶段-7部署交付)
- [全局规则](#全局规则)
- [术语表](#术语表)

---

## Phase 0：前置检查与路由

**目标**：确认环境、推断进度、选择模式，再进入正式阶段。

进入阶段 1 之前，依次执行：

### 1. 工作目录状态

- **空目录** → 告知将在此创建项目，继续
- **已有代码** → 询问：从零开始（是否可覆盖）还是基于现有代码继续；若继续，先读取结构并适配后续阶段
- **仅有 docs / 部分产物** → 进入进度恢复逻辑

### 2. Git 状态

运行（或等价检查）：`git status`；若非仓库则提示。

- **未初始化** → 询问是否初始化（**建议**初始化，不强制）
- **已初始化且有未提交改动** → 提醒先提交或暂存，避免丢失

### 3. 进度恢复（按产物推断）

按**优先级从高到低**推断；向用户展示结果并确认，**禁止静默跳阶段**：

| 已有产物 | 推断位置 |
| -------- | -------- |
| `docs/test-report.md` 且主要 docs 齐全 | 阶段 7 或已完成，询问是否收尾/迭代 |
| `TODO.md` 有未勾选项 + `docs/architecture.md` | 阶段 5 进行中 |
| `docs/architecture.md` | 阶段 4 完成 → 可进 5 |
| `docs/techsol.md` | 阶段 3 完成 → 可进 4 |
| `docs/design.md` 或 `docs/ui-prototypes/` | 阶段 2 完成 → 可进 3 |
| `docs/prd.md` | 阶段 1 完成 → 可进 2 |
| 无上述产物 | 从阶段 1 开始（或先 brainstorm） |

若存在 `docs/checkpoint.md`，一并读取作辅助，**不以 checkpoint 单独覆盖产物推断**，冲突时以产物表 + 用户确认为准。

### 4. 模式选择

询问用户：

- **Full Flow（默认）**：完整 7 阶段
- **Fast Track（MVP）**：压缩文档与原型，仅 P0（见下文）；须**显式同意**

### 5. 想法状态

- 已有明确想法 → 进入阶段 1 收集细节
- 需要 brainstorm → 先问领域、受众、约束，收敛后再进阶段 1

### Phase 0 确认点

- [ ] 工作目录策略已确认
- [ ] Git 策略已确认（初始化 / 暂不 / 先清理）
- [ ] 进度推断已展示并经用户确认
- [ ] Full Flow 或 Fast Track 已确认
- [ ] 想法是否清晰已确认

---

## 模式：Full Flow 与 Fast Track

### Full Flow

执行下方阶段 1–7 的完整步骤与产出。

### Fast Track（MVP）

**仅当用户明确同意**时启用。可随时升级回 Full Flow（补齐文档后继续）。

| 阶段 | Fast Track 行为 |
| ---- | --------------- |
| 1 | 压缩提问：用户、问题、形态、P0 功能、成功标准 |
| 2 | 可跳过完整 HTML 原型；输出 design 要点 + 关键线框/文字布局 |
| 3 | 推荐默认栈；简化部署与集成 |
| 4 | 精简 architecture：栈 + 目录 + 禁改清单 + 验收 |
| 5 | `TODO.md` 仅 P0 |
| 6 | 主流程 + 关键异常；可标注已知限制 |
| 7 | 最小 README + 文档与代码一致检查 |

---

## 阶段 1：产品定位

**目标**：理解意图，产出可落地的 PRD。

### 流程

1. **每次只问 1–2 个问题**（避免过载）。Full Flow 问题清单：
   - 目标用户是谁？
   - 使用场景是什么？
   - 主要解决什么问题？
   - 产品形态？（网站 / 小程序 / App / 桌面 / 浏览器插件 / API / CLI / 其它）
   - 核心功能及优先级（P0/P1/P2）
   - 成功指标（转化、日活、效率等）
   - 预期用户规模（影响架构）
   - UI 风格倾向（可给参考产品）

   Fast Track 可只覆盖：用户、问题、形态、P0、成功标准。

2. 整理 **PRD 草稿**，结构：
   - 产品概述
   - 目标用户与画像
   - 核心使用场景
   - 核心功能列表（P0/P1/P2）
   - 产品形态及平台
   - UI 风格倾向
   - 非功能性需求（性能、安全、可访问性、国际化等，按需）
   - 关键决策与理由
   - 成功指标与目标
   - 预估用户规模
   - 约束与假设

3. **PRD 自检**（展示给用户前自行过一遍）：
   - [ ] 核心问题是否清晰
   - [ ] 用户画像是否可辨
   - [ ] P0/P1/P2 是否明确
   - [ ] 成功指标是否尽量可量化
   - [ ] 约束与假设是否写出

4. 逐项确认后写入 **`docs/prd.md`**（后续唯一需求依据）。

### 产出物

- `docs/prd.md`

### 确认点

- [ ] 关键问题已回答（或 Fast Track 最小集）
- [ ] PRD 已确认
- [ ] `docs/prd.md` 已写入

---

## 阶段 2：视觉设计

**目标**：可感知的界面方案 + 设计规范。（无 UI 的 CLI/API 产品：跳过 HTML 原型，用「交互/命令/响应约定」替代，仍写简版 `design.md` 或 `docs/ux-notes.md` 并注明。）

### 流程

1. **布局方案**：基于 PRD 给出 **3 种**布局（导航 / 内容 / 操作），请用户选一
2. **交互流程**：用 **Mermaid** 画页面/状态与跳转；不畅则改图直至确认
3. **风格与色彩**：**2–3 套**配色（主色/辅色/文字色示例），请用户确认
4. **高保真原型**（Full Flow）：主要页面生成可交互 **HTML/CSS/JS**（一页一文件，可内联），目录 **`docs/ui-prototypes/`**  
   须覆盖：正常、加载、空数据、错误；适用时考虑桌面/移动  
   Fast Track：可用线框 + 要点描述代替完整 HTML
5. **设计规范** → **`docs/design.md`**：
   - 设计原则
   - 配色（主/辅/文字/功能色）
   - 字体与层级
   - 各页要点
   - 通用交互（按钮、表单、加载、空、错、弹窗）
   - 响应式（如适用）
   - 可访问性要点（对比度、焦点、语义化、`alt` 等，按产品形态取舍）
   - 关键设计决策记录

### 产出物

- `docs/ui-prototypes/`（Full Flow 或用户要求时）
- `docs/design.md`

### 确认点

- [ ] 布局已选
- [ ] 流程图已确认
- [ ] 配色已确认
- [ ] 原型或 Fast Track 替代物已确认
- [ ] `docs/design.md` 已确认

---

## 阶段 3：技术方案

**目标**：可交付开发的技术架构，**冻结选型**。

### 流程

1. 询问技术偏好；无偏好则按产品形态推荐，并说明理由
2. 写入 **`docs/techsol.md`**，须含：
   - 整体架构（**Mermaid** + 文字）
   - 各层选型及理由（关键项尽量 2–3 候选对比）
   - 数据模型（Mermaid ER 或等价结构）
   - 部署架构（含图，可简化）
   - 集成方案（第三方、支付、推送等，如有）
   - 安全要点（认证授权、传输与存储、常见 Web 风险防护等，按形态取舍）
   - 性能预算（如有 UI/API：首屏、p95 等目标）
   - 密钥与敏感信息：环境变量、**`.env.example`**、`.gitignore` 含 `.env`
   - 日志/监控等其它要点
   - **外部资源需求清单**（API Key、账号、数据源）
3. 确认通过前可多轮修改；通过后视为选型冻结（变更走全局回溯）

### 产出物

- `docs/techsol.md`
- `.env.example`（若已识别环境变量）

### 确认点

- [ ] 技术偏好或推荐方案已确认
- [ ] 架构与数据模型已确认
- [ ] 安全/性能/外部资源已过目
- [ ] `docs/techsol.md` 已写入

---

## 阶段 4：准备开发

**目标**：固化「开发宪法」，防止失控发挥。

### 流程

1. 确认已有：`prd.md`、`design.md`（或等价）、`techsol.md`；（Full Flow UI 产品）`ui-prototypes/` 或已记录跳过原因
2. 编写 **`docs/architecture.md`**，至少包括：
   - 技术栈清单（含版本要求或范围）
   - 目录结构约定
   - 数据模型概要（映射到代码）
   - 服务层约定（路由风格、模块划分、状态管理等）
   - **AI 引用规则**：仅可使用技术栈已声明依赖；新增依赖须先改文档并确认
   - 开发约束（命名、风格、注释、提交信息约定）
   - 环境变量管理规范
   - 版本控制策略（建议每 TODO 一 commit；不强制 force push / 危险重置，除非用户明确要求）
   - **禁止破坏的核心逻辑**（认证、支付、权限等）
   - 验收标准
3. 明确告知：核心文档已锁定；变更须先更新文档再开发

### 产出物

- `docs/architecture.md`
- 补全 `.env.example`（若尚未）

### 确认点

- [ ] 前置文档齐全或差异已说明
- [ ] `docs/architecture.md` 已确认
- [ ] 用户确认「核心文档锁定」

---

## 阶段 5：正式开发

**目标**：按依赖顺序小步交付。

### 流程

1. 读齐 `docs/` 规范，生成根目录 **`TODO.md`**。任务须可独立交付与测试，并标注依赖与规模，例如：

   ```markdown
   - [ ] 任务 N：名称
     - 依赖：任务 X, Y
     - 规模：小/中/大
     - 备注：
   ```

2. 声明：严格按 TODO 顺序，**一次一项**；每项开始前输出六项并等待确认。

3. **每轮必出六项模板**（确认前禁止写业务代码）：

   ```text
   📌 修改目标：
   📁 允许修改范围：
   🚫 不允许破坏的逻辑：
   ⚡ 对其它模块的影响：
   ✅ 验收标准：
   🧪 测试计划：
   ```

4. 实现后验收（按任务裁剪，但不可只测「快乐路径」）：
   - 主流程
   - 加载中（如适用）
   - 空数据（如适用）
   - 错误/异常
   - 单元测试或约定的手工检查
   - 可访问性/键盘路径（如适用）

5. 通过后：勾选 `TODO.md`；**建议** `git commit`（遵从用户仓库习惯）

### 任务失败处理

1. 优先在当前任务内修复（建议最多 3 轮）
2. 仍失败 → 说明原因；在用户同意下回滚到上一稳定点，或标记 `[阻塞]` 并记原因
3. 超出能力 → 明确请求用户决策，不假装完成

### 检查点（建议）

阶段或会话结束时，可写 **`docs/checkpoint.md`**：当前阶段、已完成 TODO、关键决策、下一步。便于恢复，**不替代**产物推断表。

### 产出物

- `TODO.md`
- 功能代码
- `docs/checkpoint.md`（建议）

### 确认点

- [ ] `TODO.md` 已确认
- [ ] 每任务六项已确认
- [ ] 验收已通过或阻塞已记录

---

## 阶段 6：系统测试

**目标**：达到可交付质量门槛。

### 流程

1. **代码审查**：健壮性、逻辑错误、与 architecture 一致性；检查未声明依赖与可疑 API「幻觉」
2. **安全**：依赖审计（如 `npm audit` 等，按栈选择）；硬编码密钥；常见注入/XSS/CSRF（Web）
3. **性能**（如适用）：对照 techsol 预算；前端可用 Lighthouse 等，达不到则记录
4. **测试**：跑已有测试；核心缺口补测或明确手工清单
5. **需求回溯**：对照 `docs/prd.md` 功能点；遗漏则补做或写入已知问题
6. **端到端**：主路径 + 异常 + 边界
7. 写入 **`docs/test-report.md`**：范围、安全、性能、测试结果、PRD 回溯、已知问题

Fast Track：至少主流程 + 关键异常 + PRD P0 回溯。

### 产出物

- `docs/test-report.md`

### 确认点

- [ ] 审查与安全检查已完成（或限制已记录）
- [ ] 测试/手工验收结果已记录
- [ ] PRD 回溯已完成
- [ ] `docs/test-report.md` 已确认

---

## 阶段 7：部署交付

**目标**：代码与文档一致，可交接、可部署。

### 流程

1. **反扫代码**，更新 `docs/architecture.md`（真实目录、环境变量、部署方式、模块说明）
2. **CI/CD**（如适用且用户需要）：生成最小可运行工作流，含测试/构建/部署要点
3. **API 文档**（如适用）：OpenAPI 或等价说明
4. **部署验证**（环境允许时）：健康检查 + 冒烟；不允许则给手工步骤
5. **项目 `README.md`**：简介、栈、快速开始、目录、环境变量、部署、贡献
6. **Git**：在用户授权下提交；建议标签如 `v1.0.0`；**禁止**未经授权的 force push / 历史重写
7. 通知：产品文档与代码已对齐，可按部署指南上线

### 产出物

- 更新后的 `docs/architecture.md`
- 项目 `README.md`
- CI/CD 与 API 文档（如适用）
- 版本标签（经用户同意）

### 确认点

- [ ] 文档已与代码同步
- [ ] README 已就绪
- [ ] 部署/验证说明已给出
- [ ] 提交与标签策略已与用户确认

---

## 全局规则

### 修改回溯

任意时刻修改更早阶段内容时：

1. 更新对应 `docs/*`（及受影响的 `TODO.md`）
2. 重新取得用户确认
3. 列出下游需同步的文档/代码
4. 再继续当前工作

### 需求冲突

发现陈述与已确认文档冲突时：

1. 暂停
2. 指出冲突并请用户选择
3. 按**最新确认**更新文档后继续

### 会话恢复

用户说「恢复进度」/「Resume progress」或 Phase 0 检测到产物时：按产物表推断 → 读 checkpoint/TODO（如有）→ **汇报并确认** → 从约定阶段继续。

### 终止与重置（须确认）

- **重新开始**：建议将进行中 docs 移至 `docs/archive/` 后回 Phase 0
- **放弃当前方案**：存档后停止
- **换方向**：更新 PRD 并视影响重走相关阶段门禁

### 失败与降级

| 情况 | 处理 |
| ---- | ---- |
| 用户拒绝方案 | 修订再确认，不跳过门禁 |
| 测试失败 | 当前任务内修；不擅自扩大范围 |
| 环境无法跑测试 | 写入 test-report，给手工步骤 |
| 缺少外部 Key | 外部资源清单 + mock/跳过并文档化 |
| 平台缺少某能力 | 降级为纯文本步骤（如无法开浏览器则说明如何本地打开原型） |

### 输出质量

- Mermaid 可渲染
- HTML 原型可本地打开预览
- 代码应可运行并通过约定验收
- 文件 UTF-8
- 不提交真实密钥

### 单任务聚焦

禁止一次性无关大改；禁止引入未在技术栈声明的依赖（除非先走文档确认）。

---

## 术语表

| 术语 | 说明 |
| ---- | ---- |
| **P0/P1/P2** | 功能优先级；P0 为 MVP 必须 |
| **PRD** | 产品需求文档 |
| **MVP** | 最小可行产品 |
| **Full Flow** | 完整 7 阶段 |
| **Fast Track** | MVP 压缩路径 |
| **Mermaid** | 文本图表语法 |
| **高保真原型** | 接近终态的可交互界面稿 |
| **CI/CD** | 持续集成/部署 |
| **OWASP Top 10** | 常见 Web 安全风险参考 |
| **Lighthouse** | 网页质量评估工具 |
| **WCAG** | 无障碍内容指南 |
| **ER 图** | 实体关系图 |
| **冒烟测试** | 核心路径快速验证 |
| **门禁** | 阶段退出前的用户确认点 |
