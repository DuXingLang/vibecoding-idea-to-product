---
name: vibecoding-idea-to-product
description: 从产品想法到交付上线的全流程规范，包含 Phase 0 前置检查、PRD、UI 原型、技术方案、小步迭代开发、测试与部署。
triggers:
  - 我有一个想法
  - 我想做一个产品
  - 帮我从零开发一个
  - 我要从想法到上线
  - 我们一起做个产品吧
  - 从零开始做一个项目
  - 恢复进度
---

# 从想法到产品全流程

当开发者提出一个产品想法，并希望基于该想法开发完整产品时，请严格按以下流程推进。

**核心原则**：
- 每阶段必须获得开发者明确确认后，方可进入下一阶段
- 所有产出文档统一放在项目根目录的 `docs/` 文件夹内
- 遇到不确定的情况，先暂停并向开发者确认

---

## 目录

- [Phase 0：前置检查](#phase-0前置检查)
- [阶段 1：产品定位](#阶段-1产品定位)
- [阶段 2：视觉设计](#阶段-2视觉设计)
- [阶段 3：技术方案](#阶段-3技术方案)
- [阶段 4：准备开发](#阶段-4准备开发)
- [阶段 5：正式开发](#阶段-5正式开发)
- [阶段 6：系统测试](#阶段-6系统测试)
- [阶段 7：部署上线](#阶段-7部署上线)
- [全局规则](#全局规则)
- [术语表](#术语表)

---

## Phase 0：前置检查

**目标**：确认工作环境就绪，避免因环境问题导致后续流程中断。

进入阶段一之前，AI 必须依次执行以下检查：

### 1. 检查工作目录状态

查看当前目录是否已有文件：

- **空目录** → 继续流程，告知开发者将在此目录创建项目
- **已有代码** → 询问开发者："检测到目录中已有代码，你希望从零重新开始，还是基于现有代码继续开发？"
  - 从零开始 → 确认是否可覆盖现有文件
  - 基于现有代码 → 记录现有代码结构，调整后续流程适配

### 2. 检查 Git 仓库

```bash
git status 2>/dev/null || echo "not a git repository"
```

- **未初始化** → 询问开发者："需要初始化 Git 仓库吗？"（建议初始化）
- **已初始化** → 检查是否有未提交的改动，如有则提醒开发者先提交或暂存

### 3. 检查 docs/ 目录

- 如果 `docs/` 目录已存在且包含文档文件 → 询问开发者是否恢复之前的进度
- 如果开发者选择恢复 → 读取 `docs/checkpoint.md` 和 `docs/` 下的现有文档，判断当前进度，从对应阶段继续
- 如果 `docs/` 不存在或为空 → 正常从阶段一开始

### 4. 确认产品想法

在进入阶段一之前，先确认开发者是否已有明确的产品想法：

- "你已经有明确的产品想法，还是需要我们一起 brainstorm 一个？"
- 如需 brainstorm → 询问方向领域、兴趣点，帮助聚焦

### 产出物

- 确认工作目录状态
- 确认 Git 仓库状态
- 确认是否恢复进度

### 确认点

- [ ] 工作目录状态已确认
- [ ] Git 仓库已初始化（或开发者选择暂不初始化）
- [ ] 是否恢复进度已明确

---

## 阶段 1：产品定位

**目标**：充分理解开发者意图，产出可落地的产品需求说明书。

### 流程

1. 依次向开发者提出以下问题，每次只问 1-2 个，避免信息过载：

   - 产品的目标用户是谁？
   - 目标用户的使用场景是什么？
   - 这个产品主要解决什么问题？
   - 你希望产品是什么形态？（网站 / 小程序 / App / 桌面应用 / 浏览器插件 / API 服务 / 命令行工具 / 其它）
   - 产品需要哪些核心功能？请按优先级列出。
   - 这个产品要达到什么样的目的或成功指标？（如转化率、日活、效率提升等）
   - 预期用户量大概是多少？（影响架构与选型）
   - 你偏好什么样的 UI 风格？（极简、科技暗黑、活泼渐变、毛玻璃、企业专业等，可提供参考产品）

2. 根据回答整理成 PRD 草稿，结构如下：

   - 产品概述
   - 目标用户与画像
   - 核心使用场景
   - 核心功能列表（P0/P1/P2）
   - 产品形态及平台
   - UI 风格倾向
   - **非功能性需求**（性能要求、安全性要求、可访问性、国际化需求）
   - **关键决策与理由**（记录产品定位过程中的重要决策及其原因）
   - 成功指标与目标
   - 预估用户规模
   - 约束与假设

3. **PRD 自检清单**（AI 生成后自行检查，确保完整性）：

   ```markdown
   - [ ] 产品解决了什么核心问题？
   - [ ] 目标用户画像是否清晰？
   - [ ] 功能优先级是否明确（P0/P1/P2）？
   - [ ] 非功能性需求是否已考虑？
   - [ ] 成功指标是否可量化？
   ```

4. 将草稿展示给开发者，逐项确认。确认完毕后，生成 `docs/prd.md`，作为后续的唯一需求依据。

### 产出物

- `docs/prd.md` — 产品需求说明书

### 确认点

- [ ] 所有需求问题已回答
- [ ] PRD 草稿已逐项确认
- [ ] `docs/prd.md` 已生成

---

## 阶段 2：视觉设计

**目标**：将需求转化为可交互的高保真原型，并沉淀设计规范。

### 流程

1. **布局方案选择**
   基于 PRD，用文字或简单框线图描述 3 种整体页面布局方案（侧重导航、内容区、操作区分布）。请开发者选择一种布局方案作为基础。

2. **交互流程确认**
   基于选定布局，使用 Mermaid 绘制整个产品的 Workflow 流程图（页面节点 + 跳转关系）。
   和开发者一起审查流程，发现逻辑不畅直接在 Mermaid 中调整，直至确认。

3. **风格与色彩确认**
   给出 2-3 套配色方案及风格描述（附主色、辅色、文字色示例），供开发者选择确认。

4. **高保真原型制作**
   根据以上确认项，为产品主要页面生成可交互的 HTML/CSS/JS 原型（一个页面一个 HTML 文件，内联样式和基础交互）。原型文件集中存放于 `docs/ui-prototypes/` 目录。
   原型应覆盖关键交互流程，并体现：
   - 正常状态
   - 加载中状态
   - 空数据状态
   - 错误提示状态
   - **响应式布局适配**（如适用，至少覆盖桌面和移动端）

5. **编写设计规范**
   确认原型后，生成 `docs/design.md`，内容包括：

   - 整体设计原则
   - 配色方案（主色、辅色、文字色、功能色）
   - 字体与排版层级
   - 各页面 UI 设计要点
   - 通用交互规范（按钮、表单、加载、空状态、错误、弹窗等）
   - 响应式与适配说明（如适用）
   - **可访问性原则**：
     - 色彩对比度符合 WCAG AA 标准（普通文本对比度 ≥ 4.5:1）
     - 焦点管理（键盘 Tab 导航顺序合理）
     - 语义化 HTML 标签（`<nav>`, `<main>`, `<button>` 等）
     - 图片 `alt` 文本
   - **设计决策记录**（关键设计选择的理由，如"为什么选择这种布局"、"为什么用这种配色"）

### 产出物

- `docs/ui-prototypes/` — 高保真交互原型（HTML 文件）
- `docs/design.md` — 设计规范文档

### 确认点

- [ ] 布局方案已选择
- [ ] 交互流程图已确认
- [ ] 配色方案已确认
- [ ] 高保真原型已审查通过
- [ ] `docs/design.md` 已生成并确认

---

## 阶段 3：技术方案

**目标**：设计可直接交付开发的技术架构，冻结技术选型。

### 流程

1. 首先询问开发者："你是否有偏好的技术栈（前端框架、后端语言、数据库等）？如无偏好，我将根据产品形态推荐最佳方案。"

   ```mermaid
   graph TD
       A[开始阶段三] --> B{开发者有技术偏好?}
       B -->|是| C[收集技术偏好]
       B -->|否| D[根据产品形态推荐最佳方案]
       C --> E[设计系统技术方案]
       D --> E
       E --> F[生成 docs/techsol.md]
       F --> G{开发者确认通过?}
       G -->|否| E
       G -->|是| H[进入阶段四]
   ```

2. 结合 PRD 与开发者偏好，设计系统技术方案，生成 `docs/techsol.md`，内容必须包含：

   - **系统整体技术架构**（用 Mermaid 架构图 + 文字说明）
   - **各层技术选型及理由**（前端、后端、数据库、中间件、部署平台等）
     - **选型对比表**：每个关键选型至少列出 2-3 个候选方案，附对比理由
   - **数据模型设计**（核心实体关系图或结构化定义，用 Mermaid ER 图或代码块表示）
   - **部署架构**（服务器、容器、CDN、CI/CD 等，附部署图）
   - **集成方案**（第三方服务、支付、消息推送等）
   - **安全考虑清单**：
     - 认证与授权方案（JWT / Session / OAuth）
     - 数据加密（传输层 TLS、存储层加密）
     - XSS / CSRF 防护方案
     - 输入校验与参数化查询（防 SQL 注入）
   - **性能预算**：
     - 首屏加载时间目标（建议 < 2s）
     - API 响应时间目标（建议 < 200ms p95）
     - 静态资源体积预算
   - **密钥与敏感信息管理**：
     - 所有密钥使用环境变量管理，不得硬编码
     - 生成 `.env.example` 文件列出所有环境变量
     - 确保 `.gitignore` 包含 `.env`
   - **其它技术要点**（日志、监控、错误追踪等）
   - **外部资源需求清单**（需开发者提供的 API Key、外部接口、数据源等）

3. 将方案与开发者确认，有调整则修改直至通过。

### 产出物

- `docs/techsol.md` — 技术实现方案文档

### 确认点

- [ ] 技术栈偏好已确认
- [ ] 技术架构方案已确认
- [ ] 数据模型设计已确认
- [ ] 安全方案与性能预算已确认
- [ ] `docs/techsol.md` 已生成

---

## 阶段 4：准备开发

**目标**：建立不可轻易变更的规范边界，防止 AI 编程失控。

### 流程

1. 确保 `docs/` 目录下已有：`prd.md`、`design.md`、`techsol.md`、`ui-prototypes/` 文件夹。

2. 基于以上三份文档，编写 `docs/architecture.md`，作为开发的"宪法"，内容至少包括：

   - **技术栈清单**（最终确定的框架、库、工具及版本要求）
   - **目录结构约定**（项目文件组织规范）
   - **数据模型概要**（映射到代码的结构约定）
   - **服务层约定**（API 路由风格、服务划分、状态管理策略）
   - **AI 引用规则**（严格限定可引用的库和模块，禁止引入未在技术栈中声明的依赖）
   - **开发约束**（命名规范、代码风格、注释要求）
   - **环境变量管理规范**：
     - 列出所有环境变量及其用途
     - 生成 `.env.example` 文件
     - 规定 `.env` 文件不得提交到版本控制
   - **版本控制策略**：
     - 每完成一个 TODO 任务，执行一次 `git commit`
     - 提交信息格式：`<type>: <description>`（如 `feat: 实现用户注册`）
     - 每完成一个阶段，打一个阶段标签（如 `phase-1-prd`）
     - 紧急回滚：`git reset --hard <last-known-good-commit>`
   - **禁止破坏的核心逻辑**（明确哪些流程或模块不可擅自修改，如认证逻辑、支付流程）
   - **验收标准**（功能与质量要求）

3. 告知开发者："核心文档已锁定，后续开发将严格以此为依据，任何变更需先更新对应文档。"

### 产出物

- `docs/architecture.md` — 开发架构规范文档
- `.env.example` — 环境变量模板

### 确认点

- [ ] 所有前置文档已就绪
- [ ] `docs/architecture.md` 已生成并确认
- [ ] 版本控制策略已确认
- [ ] 开发者确认"核心文档已锁定"

---

## 阶段 5：正式开发

**目标**：按任务清单顺序，安全可控地完成所有模块开发。

### 流程

1. 读取 `docs/` 下所有规范文档，在项目根目录生成 `TODO.md`，将开发工作拆解为可独立交付、可测试的任务模块。

   **TODO.md 每个任务格式**：

   ```markdown
   - [ ] 任务 N：任务名称
     - 依赖关系：Depends on 任务 X, Y
     - 阻塞原因：（如无则留空）
     - 预计工作量：小/中/大
   ```

2. 告知开发者："我们将严格按照 TODO 顺序逐项开发，一次只做一件事。每个任务开始前我会明确六项内容，请确认后再执行。"

3. **每一轮迭代的对话模式**（AI 必须输出以下内容并等待确认）：

   ```
   📌 修改目标：本任务完成什么
   📁 允许修改范围：可修改的文件或模块列表
   🚫 不允许破坏的逻辑：参照 architecture.md 中的核心逻辑
   ⚡ 对其它模块的影响：预估影响范围及缓解措施
   ✅ 验收标准：如何算完成任务
   🧪 单元测试计划：打算如何验证
   ```

4. 开发者确认后，AI 方可编写代码。

5. 修改完成后，必须执行以下验收测试：

   - 主流程测试
   - 加载中状态
   - 空数据状态
   - 接口报错/异常状态
   - 单元测试（若任务中包含）
   - **可访问性检查**（键盘导航是否可用、焦点顺序是否正确）

   全部通过后，在 `TODO.md` 中勾选该任务，并执行 `git commit`。

### 任务失败处理

如果某个任务开发中遇到严重问题（测试不通过、引入 bug、无法按预期完成）：

1. **修复尝试**：优先在当前任务内修复，最多尝试 3 轮
2. **回滚**：3 轮后仍无法解决 → 执行 `git reset --hard` 回滚到上一个任务完成时的状态
3. **跳过**：如果确定当前任务暂时无法完成 → 在 TODO.md 中标记为 `[阻塞]`，记录原因，进入下一个任务
4. **求助**：如果问题超出 AI 能力范围 → 明确告知开发者当前无法解决的问题，请求人工介入

### 上下文管理

- 每个阶段完成时，建议开发者："建议在此处保存并重新开始新会话，以保持上下文清晰。下次启动时，我会读取 `docs/` 下的文档自动恢复进度。"
- 当前阶段结束时，自动生成 `docs/checkpoint.md`，记录当前进度、已完成任务、关键决策

### 产出物

- `TODO.md` — 任务清单与进度追踪
- 功能代码（增量提交）
- `docs/checkpoint.md` — 阶段检查点（可选）

### 确认点

- [ ] TODO.md 已生成并确认
- [ ] 每轮迭代的 6 项内容已确认
- [ ] 验收测试已通过
- [ ] 任务失败已按流程处理（如有）
- [ ] 代码已提交到 Git

---

## 阶段 6：系统测试

**目标**：确保产品质量达到可交付标准。

### 流程

1. **全量代码审查**
   - 检查代码健壮性、潜在逻辑错误
   - 安全漏洞检查（参考 OWASP Top 10 清单）
   - **AI 幻觉检查**：验证代码中引用的 API 是否真实存在、库版本是否正确、引入的依赖是否在技术栈中声明过
   - 修复发现的问题

2. **安全扫描**
   - 运行 `npm audit` 或等效命令检查依赖安全漏洞
   - 检查硬编码密钥或敏感信息泄露
   - 对 Web 应用，检查 XSS、CSRF、SQL 注入防护是否到位

3. **性能基准测试**
   - 前端：运行 Lighthouse 性能评分（目标 ≥ 80）
   - 后端：检查 API 响应时间是否达到阶段三定义的性能预算
   - 记录性能数据到测试报告

4. **单元测试执行**
   - 运行所有已有单元测试
   - 对缺失覆盖的核心逻辑补充测试用例
   - 确保全部通过

5. **需求回溯**
   - 逐条对照 `docs/prd.md` 中的功能点，检查是否有遗漏
   - 未实现的功能立即补充或标记为已知问题

6. **整体验收**
   - 执行完整业务流程测试，覆盖正常、异常、边界情况
   - 评估是否达到可交付生产状态

7. 生成 `docs/test-report.md`，记录测试范围、通过情况与已知问题：

   ```markdown
   # 测试报告
   - 审查范围：（审查了哪些文件/模块）
   - 安全扫描结果：（通过 / 有高危漏洞需修复）
   - 性能测试结果：（Lighthouse 评分、API 响应时间）
   - 单元测试覆盖率：（X%）
   - 需求回溯结果：（P0 全部完成 / XX 项未实现）
   - 已知问题：（列表）
   ```

### 产出物

- `docs/test-report.md` — 全量测试报告

### 确认点

- [ ] 全量代码审查已完成
- [ ] 安全扫描已完成
- [ ] 性能基准测试已完成
- [ ] 所有单元测试通过
- [ ] 需求回溯已完成
- [ ] `docs/test-report.md` 已生成

---

## 阶段 7：部署上线

**目标**：确保代码与文档一致，规范化交付。

### 流程

1. **文档同步**
   - 扫描最终项目代码，将实际目录结构、部署方式、环境变量说明、核心模块实现细节更新到 `docs/architecture.md`

2. **生成 CI/CD 配置**（如适用）
   - 根据部署平台，生成对应的 CI/CD 配置文件
   - 示例：GitHub Actions 部署到 Vercel / Docker 部署到服务器
   - 至少包含：自动化测试、构建、部署步骤

3. **生成 API 文档**（如适用）
   - 扫描代码中的 API 路由和注释，生成 OpenAPI/Swagger 文档
   - 确保 API 文档可通过 `/api-docs` 或类似路径访问

4. **部署验证**
   - 执行健康检查（服务是否正常启动）
   - 执行冒烟测试（核心功能是否正常）

5. **生成 README**
   - 编写规范的 `README.md`，包含：
     - 项目简介
     - 技术栈
     - 快速开始（安装、运行、构建）
     - 目录结构
     - 环境变量说明
     - 部署指南
     - 贡献与规范

6. **Git 提交**
   - 将最终代码与文档一并提交，打上版本标签（如 v1.0.0）
   - 确保仓库干净可追溯

7. 通知开发者："产品已就绪，可随时部署上线。相关文档均已更新至最新状态。"

### 产出物

- 更新后的 `docs/architecture.md`
- `README.md` — 项目最终 README
- CI/CD 配置文件（如 `.github/workflows/deploy.yml`）
- API 文档（如适用）
- Git 版本标签（如 `v1.0.0`）

### 确认点

- [ ] 文档已同步到最新代码状态
- [ ] CI/CD 配置已生成
- [ ] API 文档已生成（如适用）
- [ ] 部署验证通过
- [ ] README.md 已生成
- [ ] Git 提交已完成并打上版本标签

---

## 全局规则

### 修改回溯

任何阶段，开发者提出修改上一阶段内容时，AI 必须：

1. 更新对应文档
2. 重新取得开发者确认
3. 再继续当前阶段

### 需求冲突仲裁

当 AI 发现新信息与已有文档冲突时（如开发者说"目标用户是普通用户"但又说"需要专业级权限管理"）：

1. 暂停当前操作
2. 向开发者指出冲突："我注意到 XX 和 YY 之间存在矛盾，请问你倾向于哪个方向？"
3. 根据开发者的回答更新对应文档
4. 按"最新确认"优先原则处理

### 会话恢复机制

当开发者说"恢复进度"或检测到现有 `docs/` 目录中有文档时：

1. 读取 `docs/checkpoint.md`（如存在），了解当前进度
2. 读取 `docs/prd.md`、`docs/design.md`、`docs/techsol.md`、`docs/architecture.md` 了解已确认的决策
3. 读取 `TODO.md`（如存在），了解任务完成情况
4. 向开发者汇报当前进度，确认是否继续
5. 从对应阶段继续推进

### 终止与重置

- **"重新开始"** → 将 `docs/` 下的进行中文件移动到 `docs/archive/`，退回 Phase 0
- **"放弃当前方案"** → 保存当前进度到 `docs/archive/`，告知开发者已存档
- **"换个方向"** → 更新 `docs/prd.md` 中的产品方向，重新从阶段一确认

### 上下文窗口管理

- 每个阶段完成时，建议开发者保存并重新开始新会话
- 重新启动时，AI 自动读取 `docs/` 下的文档恢复上下文
- 开发者可随时问"当前进度如何"，AI 查看 `docs/checkpoint.md` 和 `TODO.md` 汇报

### 输出物质量

- 所有 Mermaid 图表必须语法正确，可直接渲染
- 所有 HTML 原型必须可交互，在浏览器中可直接预览
- 所有代码必须可运行，通过验收测试
- 所有生成的文件使用 UTF-8 编码

### 单任务聚焦

始终保持"一次只做一件事"的原则，禁止 AI 自由发挥超出规范范围。

---

## 术语表

| 术语 | 说明 |
| ------ | ------ |
| **P0/P1/P2** | 功能优先级等级。P0 为必须实现（MVP），P1 为重要功能，P2 为可延后 |
| **Mermaid** | 基于文本的图表生成工具，可生成流程图、ER 图、架构图等 |
| **高保真原型** | 接近最终产品视觉和交互效果的页面原型，可交互操作 |
| **PRD** | 产品需求文档（Product Requirements Document） |
| **MVP** | 最小可行产品（Minimum Viable Product），包含最核心功能的最小版本 |
| **CI/CD** | 持续集成/持续部署（Continuous Integration / Continuous Deployment） |
| **OWASP Top 10** | OWASP 组织发布的 Web 应用十大安全风险清单 |
| **Lighthouse** | Google 推出的网页质量评估工具，涵盖性能、可访问性等 |
| **WCAG** | Web 内容可访问性指南（Web Content Accessibility Guidelines） |
| **OpenAPI/Swagger** | RESTful API 文档规范与工具集 |
| **ER 图** | 实体关系图（Entity-Relationship Diagram），用于数据模型设计 |
| **冒烟测试** | 核心功能的快速验证测试，确保系统基本可用 |
| **CRUD** | 增删改查（Create, Read, Update, Delete），最基本的数据库操作 |
